export const queryBuilderGuidance = {
  ruleConcept:
    "'{X}' has been selected. This is a parent concept - all included child concepts are listed so that you may refine your query further if required.",
  ruleAge:
    "Setting Age > X to 'Persons' means: 'Include only patients who are currently older than X.' \n If you want the age at the time of an event, you must add the age to the event (e.g. a condition, a drug, a measurement)",
};
