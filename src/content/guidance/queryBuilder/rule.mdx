{/**
  * @typedef Props
  * @property {string} [category]
  *  OMOP domain name
  * @property {[string | null, string | null] | undefined} timeConstraint
  * @property {[number | null, number | null] | undefined} ageConstraint
  * @property {[number | null, number | null] | undefined} valueAsNumber
  * @property {boolean} showSelectors
  */}


<ToggleExclusion/>

{/* - not available yet

# Descendants

<ShowDescendants/>

*/}

# {props.showSelectors && <>Rule Parameters</>}

<Box display="flex" flexDirection="column" gap={1} alignItems="flex-start">
  {props.showSelectors &&
    <>
      <AddTimeFrameButton label="Add timeframe" />
      <AddAgeButton label="Add age" />
    </>
  }
</Box>

<Box>
  {props.showSelectors &&
    <>
      <CollapsibleGuidance keyPrefix="RuleTimeframeSelector" title={props.timeConstraint && `Set Timeframe`}>
        <RuleTimeframeSelector>
          <DeleteTimeFrameButton label="Remove timeframe from this rule" />
        </RuleTimeframeSelector>
        <Box>
          Set the timeframe {props.category ? <>when the {props.category.toLowerCase()} event occurred</> : <>for this rule</>}.
        </Box>
      </CollapsibleGuidance>

      <CollapsibleGuidance keyPrefix="RuleAgeSelector" title={props.ageConstraint && `Set Age`}>
        <RuleAgeSelector>
          <DeleteAgeButton label="Remove age from this rule" />
        </RuleAgeSelector>
        <Box>
          {props.category ? <>Define the patient's age when the {props.category.toLowerCase()} event occurred.</> : <>Define the patient age range.</>}
          <Box sx={{ color: "error.main" }}>
            Age may be rounded ±1 year in some datasets, to either protect patient data or as specific day or month was not recorded.
          </Box>
        </Box>
      </CollapsibleGuidance>

      {props.valueAsNumber && (
        <CollapsibleGuidance keyPrefix="RuleValueAsNumberSelector" title="Set Value">
          <RuleValueAsNumberSelector>
            <DeleteValueAsNumberButton label="Remove value from this rule" />
          </RuleValueAsNumberSelector>
          <Box>
            Filter patients by their recorded numeric value for this concept. Use <strong>≥</strong> or <strong>&lt;</strong> for a one-sided bound, or <strong>↔</strong> to set a range with both a lower and upper limit.
          </Box>
          <Box sx={{ color: "error.main" }}>
            This is a filter on the raw measurement value, at current it does not taking into account the units of the measurement
          </Box>
        </CollapsibleGuidance>
      )}
    </>
  }
</Box>