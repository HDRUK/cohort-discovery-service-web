
{/**
  * @typedef Props
  * @property {boolean} creating
  *  flag if is creating
  */}


{props.creating ? (
  <>
  A host is a connection point that allows the system to access and manage your collections. When a host is created, the system automatically generates a Client ID and Client Secret to securely authenticate with the service. You will need this host whenever you create a collection, as it connects the collection to its source. 
  </>
  ):(
    <>
    Collection hosts information  
    will appear here when you select one in the middle panel...
    </>) 
}