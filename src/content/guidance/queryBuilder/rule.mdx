{/**
  * @typedef Props
  * @property {string} category
  * @property {string} verb
  * @property {string} verbPastTense
  *  OMOP domain name
  */}

# Include/Exclude

<ToggleExclusion/>

# Include/Exclude Guidance

Toggle <b>Including</b> to add items that meet the selected criteria. Use this to include them in your results.

Toggle <b>Excluding</b> to remove items that meet the selected criteria. Use this to exclude them from your results.

# Guidance

Concept refinements - start broad (parent concept) or narrow down to specific subtypes
(child concepts).

# Rule Parameters

<AddTimeFrameButton label={`Add timeframe when ${props.category.toLowerCase()} was ${props.verb}`} />

<AddAgeButton label={`Add age of patient when ${props.category.toLowerCase()} was ${props.verb}`} />



<RuleTimeframeSelector title={`Timeframe of when the ${props.category} was ${props.verb}`}>
<DeleteTimeFrameButton label={`Remove timeframe constraint for this ${props.category}`} />
</RuleTimeframeSelector>

# Timeframe Guidance

Set the time frame for this rule — the window when the condition occurred/measurement was recorded/medication was prescribed.

<RuleAgeSelector title={`Age of person when the ${props.category} was ${props.verb}`}>
<DeleteAgeButton label={`Remove age of person constraint for this ${props.category}`} />
</RuleAgeSelector>

# Age at Diagnosis/ Procedure Occurrence/ at Time of Measurement/ Drug Intake Guidance

Define the patient’s age at diagnosis. Age may be rounded ±1 year in some datasets, to either protect patient data or as specific day or month was not recorded, so consider this when setting the range for your research question.


