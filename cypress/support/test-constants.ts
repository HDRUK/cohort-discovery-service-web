/**
 * Fixture-level constants shared across Cypress specs.
 * Values must stay in sync with the corresponding fixture files.
 */

/** PID of the primary test custodian — matches custodians.json[0].pid */
export const CUSTODIAN_PID = "cust-pid-1";

/** PID of the primary test query — matches query.json .data.pid */
export const QUERY_PID = "query-pid-1";
