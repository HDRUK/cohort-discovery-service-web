{/**
  * @typedef Props
  * @property {string} category
  * @property {string} verb
  * @property {string} verbPastTense
  *  OMOP domain name
  */}

<CollapsibleGuidance keyPrefix="ToggleExclusion" title="Include/Exclude">
  <ToggleExclusion/>
  <Box>
    Toggle <b>Including</b> to add items that meet the selected criteria.

    Toggle <b>Excluding</b> to remove items that meet the selected criteria.
  </Box>
</CollapsibleGuidance>
{/* - not available yet

# Descendants

<ShowDescendants/>

*/}

# Rule Parameters

<AddTimeFrameButton label={`Add timeframe when ${props.category.toLowerCase()} was ${props.verb}`} />

<AddAgeButton label={`Add age of patient when ${props.category.toLowerCase()} was ${props.verb}`} />

<CollapsibleGuidance keyPrefix="RuleTimeframeSelector" title={`Timeframe of Condition Diagnosis`}>
  <RuleTimeframeSelector>
    <DeleteTimeFrameButton label={`Remove timeframe constraint for this ${props.category}`} />
  </RuleTimeframeSelector>
  <Box>
    Set the time frame for this rule — the window when the condition occurred/measurement was recorded/medication was prescribed.
  </Box>
</CollapsibleGuidance>

<CollapsibleGuidance keyPrefix="RuleAgeSelector" title={`Patient Age at Condition Diagnosis`}>
  <RuleAgeSelector>
    <DeleteAgeButton label={`Remove age of person constraint for this ${props.category}`} />
  </RuleAgeSelector>
  <Box>
    Define the patient age range by adjusting slider value. Age may be rounded ±1 year in some datasets, to either protect patient data or as specific day or month was not recorded.
  </Box>
</CollapsibleGuidance>